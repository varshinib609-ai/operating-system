#include <stdio.h>

int main()
{
    FILE *fp;
    char str[50];

    fp = fopen("sample.txt","w");
    fprintf(fp,"Operating Systems Lab");
    fclose(fp);

    fp = fopen("sample.txt","r");
    fgets(str,50,fp);
    printf("%s",str);
    fclose(fp);

    return 0;
}
